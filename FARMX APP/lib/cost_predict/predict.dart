import 'package:flutter/material.dart';

class costpredict extends StatefulWidget {
  @override
  _costpredictState createState() => _costpredictState();
}

class _costpredictState extends State<costpredict> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: new Text('Cost Predict'),
      ),
      body: Center(
        child: new Text('  '),
      ),
    );
  }
}
